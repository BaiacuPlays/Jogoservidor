{
  "name": "cara-acara-mobile",
  "version": "1.1.0",
  "private": true,
  "description": "Jogo Cara a Cara otimizado para dispositivos móveis",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "analyze": "ANALYZE=true next build",
    "lighthouse": "lighthouse http://localhost:3000 --output=html --output-path=./lighthouse-report.html --chrome-flags='--headless'",
    "optimize-images": "imagemin public/imagens/* --out-dir=public/imagens/optimized --plugin=imagemin-webp --plugin=imagemin-mozjpeg"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@next/bundle-analyzer": "^14.1.0",
    "imagemin": "^8.0.1",
    "imagemin-webp": "^8.0.0",
    "imagemin-mozjpeg": "^10.0.0",
    "lighthouse": "^11.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all",
      "iOS >= 12",
      "Android >= 8"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
