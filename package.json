{
  "name": "pizzaria-del-gatito",
  "version": "1.0.0",
  "description": "Pizzaria del Gatito, the game",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
